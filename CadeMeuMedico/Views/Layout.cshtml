<!DOCTYPE html>

<html lang="pt-br">
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Cade meu medico? - @ViewBag.Title</title>
    <link href="@Url.Content("~/Content/bootstrap.css")" rel="stylesheet" />
    <style>
        body{
            padding-top: 60px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark navbar-expand-sm bg-dark fixed-top">

        <div class="container">            
            <a class="navbar-brand" href="#">Cade meu medico?</a>           
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="#"></a></li>
                    <li id="menuMedicos">
                        @Html.ActionLink("Médicos", "index", "Medicos", null, new {@class = "btn btn-dark"})
                    </li>
                    <li id="menuMedicos">
                        @Html.ActionLink("Cidades", "index", "Cidades", null, new {@class = "btn btn-dark"})
                    </li>
                    <li id="menuMedicos">
                        @Html.ActionLink("Especialidades", "index", "Especialidades", null, new { @class="btn btn-dark"} )
                    </li>
                </ul>
            </div>
        </div>    
    </nav>

    <div class="container">
        @RenderBody()
    </div>


    <script src="@Url.Content("~/Scripts/jquery-3.3.1.js")" ></script>
    <script src="@Url.Content("~/Scripts/bootstrap.js")"></script>
    @RenderSection("script", required: false)

</body>
</html>
